{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "buildCommand": "cd 'Cart Manager' && npm run build",
    "outputDirectory": "Cart Manager/dist",
    "installCommand": "cd 'Cart Manager' && npm install"
}